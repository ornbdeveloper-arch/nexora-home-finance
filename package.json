{
  "name": "nexora-financas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Gestor financeiro em HTML, CSS e JavaScript, sem dependências",
  "scripts": {
    "start": "node --env-file=.env server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}